VIM config for Ruby, Rails, Groovy and Java development.
Requried: ctags.

Used plugins:
Rails - Rails commands, easy navigation, jump to Model, View and more.
NERDTree - Improved Project/Filesystem Tree. 
FuzzyFinder - Quikly reach the buffer/file/command/tag explorer finder.
SnipMate - TextMate snippets for Vim (extensible).
Surround - Delete/Change/Add parentheses, quotes, XML tags and more.
F3 - recursive advanced regex search.

Note:
NERDTree is mapped to F2 key, show always. You can change it in .vimrc file (comment line "au VimEnter *  NERDTree").
Help - ? in NERDTree window.
